# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/opt/homebrew/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  # Honor DATABASE_HOST/PORT (set in .env to `db`:5432 for Docker). Falls back to
  # localhost:5432 so GitHub CI (Postgres service on localhost) and bare-metal
  # dev both work unchanged.
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>
  port: <%= ENV.fetch("DATABASE_PORT", 5432) %>
  username: postgres
  password: postgres

development:
  primary:
    <<: *default
    database: gabi_project_backend_development
  queue:
    url: <%= ENV["QUEUE_DATABASE_URL"] %>
    migrations_paths: db/queue_migrate

test:
  <<: *default
  database: gabi_project_backend_test

production:
  primary:
    <<: *default
    database: gabi_project_backend_production
    username: gabi_project_backend
    password: <%= ENV["GABI_PROJECT_BACKEND_DATABASE_PASSWORD"] %>
