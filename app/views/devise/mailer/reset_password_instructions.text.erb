Olá, <%= @resource.name.split(" ").first %>!

Recebemos uma solicitação para redefinir a senha da sua conta.

Acesse o link abaixo para criar uma nova senha (válido por <%= Devise.reset_password_within / 3600 %> horas):

<%= "#{ENV.fetch('FRONTEND_URL', 'http://localhost:5173')}/reset-senha?token=#{@token}" %>

Se você não solicitou a redefinição de senha, ignore este e-mail.

Atenciosamente,
Equipe Gabi
